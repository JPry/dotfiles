# COMMAND PROMPT OUTPUT

print_before_the_prompt() {
    printf "$txtred[%s] $bldblu%s $txtpur%s\n$txtrst" "$USER" "${PWD/$HOME/~}" "$(vcprompt -f '[%n: %b%m%u]')"
}
PROMPT_COMMAND=print_before_the_prompt
PS1='$ '
